import 'package:flutter/material.dart';

class AllegroResult extends StatelessWidget {
  final String mbti;

  AllegroResult({Key key, @required this.mbti}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}